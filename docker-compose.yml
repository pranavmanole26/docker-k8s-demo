version: '3'
services:
  redis-server:
    image: "redis"
  golang-app:
    build: .
    ports:
      - "8081:8080"
    volumes:
      - .:/app
  tests:
    build: .
    volumes:
      - .:/app
    depends_on:
      - redis-server
    command: ["go", "test", "-v", "./..."]
